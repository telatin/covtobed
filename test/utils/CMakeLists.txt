cmake_minimum_required(VERSION 3.10)
project(test_utils)

# Note: C++ BAM generator temporarily disabled due to BamTools API compatibility
# Using shell script generator instead (simple_test_generator.sh)